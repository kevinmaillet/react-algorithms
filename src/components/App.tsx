import React from 'react';
import BubbleSort from './BubbleSort';

const App: React.FC = () => {
  return (
    <main style={{ textAlign: `center` }}>
      App
      <div>
        <BubbleSort />
      </div>
    </main>
  );
};

export default App;
